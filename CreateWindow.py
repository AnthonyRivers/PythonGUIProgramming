#The tkinter module needs to be imported
import tkinter as tk

# We need an instance of Tk 
win = tk.Tk()

# We can set the title of the window using the 'title' method
win.title('Creating a Window GUI With Python')

#call the mainloop method to manage the event queue and render the new window
win.mainloop()
